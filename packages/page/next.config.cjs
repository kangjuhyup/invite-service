/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  basePath: '/page',
  output: 'standalone',
  reactStricMode: false,
  assetPrefix: '/page',
};

module.exports = nextConfig;
